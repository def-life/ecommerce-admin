import Header from '@/components/Header'
import React from 'react'

function categories() {
  return (
    <div><Header />The Only purpose of the project was Next-Auth(Credential Provider + OAuth), Upload images to S3 bucket AWS, Integrating Payment Gateway, try out SWE(stale while revalidate). Ignore a little bit of UI, UI takes time. No particular Design was there in my mind. Main focus was above functionalities</div>
  )
}

export default categories